{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "oauth:default",
    "oauth:allow-start",
    "oauth:allow-cancel",
    "shell:allow-open",
    "shell:allow-open",
    "store:default"
  ]
}